{% extends 'base.html.twig' %}

{% block title %}Modifier la recette{% endblock %}

{% block stylesheets %}
{{ parent() }}
<link rel="stylesheet" href="{{ asset('styles/edit.css') }}">
{% endblock %}

{% block body %}
<section class="edit-recipe">
  <h1>Modifier la recette</h1>

  {{ include('recipe/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

  <a href="{{ path('app_recipe_show', {'id': recipe.id}) }}" class="back-link">← Retour à la recette</a>

  {{ include('recipe/_delete_form.html.twig') }}
</section>
{% endblock %}

